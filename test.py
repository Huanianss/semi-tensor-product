import sklearn
import stp
import numpy as np
print(stp)
# a = np.random.random([2, 3])
# b = np.random.random([2, 3])
# c = stp.sp(a, b)
# print(c.shape)
#
# a = stp.STP(a)
# b = stp.STP(b)
# c = a @ b
# print(c.double().shape)
